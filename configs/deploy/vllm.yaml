model_path: "output/model"
tokenizer_path: "output/model"

server:
  host: "0.0.0.0"
  port: 8000
  workers: 1

generation:
  max_model_len: 4096
  temperature: 0.7
  top_p: 0.9
  dtype: "float16"

quantization:
  method: null

gpu_memory_utilization: 0.90
